---
import { testimonials } from '../../database/data';
---

<section id="testimonios" class="py-20 bg-gray-50">
  <div class="container mx-auto px-4">
    <!-- Section Header -->
    <div class="text-center mb-16">
      <h2 class="text-4xl md:text-5xl font-bold text-gray-800 mb-6">
        Lo que Dicen Nuestros Clientes
      </h2>
      <p class="text-xl text-gray-600 max-w-3xl mx-auto">
        La satisfacción de nuestros clientes es nuestra mayor recompensa
      </p>
    </div>

    <!-- Testimonials Grid -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 mb-16">
      {testimonials.map((testimonial, index) => (
        <div class="testimonial-card animate-fade-in-up" style={`animation-delay: ${index * 0.1}s`}>
          <div class="testimonial-text">
            "{testimonial.content}"
          </div>
          
          <!-- Rating -->
          <div class="flex items-center mb-4">
            {Array.from({ length: testimonial.rating }, (_, i) => (
              <svg class="w-5 h-5 text-yellow-400" fill="currentColor" viewBox="0 0 20 20">
                <path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z"></path>
              </svg>
            ))}
          </div>
          
          <div class="testimonial-author">
            <div class="testimonial-avatar">
              {testimonial.name.charAt(0)}
            </div>
            <div>
              <div class="testimonial-name">{testimonial.name}</div>
              <div class="testimonial-role">{testimonial.role}</div>
              <div class="text-sm text-gray-500">{testimonial.company}</div>
            </div>
          </div>
        </div>
      ))}
    </div>

    <!-- Stats Section -->
    <div class="bg-white rounded-2xl shadow-xl p-8">
      <div class="grid grid-cols-1 md:grid-cols-4 gap-8 text-center">
        <div class="animate-fade-in-up">
          <div class="text-4xl font-bold text-primary-600 mb-2">500+</div>
          <div class="text-gray-600">Clientes Satisfechos</div>
        </div>
        <div class="animate-fade-in-up" style="animation-delay: 0.1s">
          <div class="text-4xl font-bold text-primary-600 mb-2">1000+</div>
          <div class="text-gray-600">Servicios Completados</div>
        </div>
        <div class="animate-fade-in-up" style="animation-delay: 0.2s">
          <div class="text-4xl font-bold text-primary-600 mb-2">98%</div>
          <div class="text-gray-600">Satisfacción del Cliente</div>
        </div>
        <div class="animate-fade-in-up" style="animation-delay: 0.3s">
          <div class="text-4xl font-bold text-primary-600 mb-2">24/7</div>
          <div class="text-gray-600">Atención Disponible</div>
        </div>
      </div>
    </div>

    <!-- CTA Section -->
    <div class="text-center mt-16">
      <h3 class="text-3xl font-bold text-gray-800 mb-4">
        ¿Listo para Experimentar Nuestro Servicio?
      </h3>
      <p class="text-lg text-gray-600 mb-8">
        Únete a nuestros clientes satisfechos y descubre la diferencia de trabajar con profesionales
      </p>
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a href="#contacto" class="btn-primary">
          Solicitar Cotización
        </a>
        <a href="tel:+15551234567" class="btn-outline">
          Llamar Ahora
        </a>
      </div>
    </div>
  </div>
</section>
